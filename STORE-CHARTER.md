# Store Charter（商店定位、MVP 目标与反跑偏边界）

本文是 `zhixu-store` 的能力章程：这个仓库做什么、不做什么、按什么顺序做、新需求如何归类。
任何新功能先对照本文归类；归类不进去的，要么先修订本文（视为边界变更，需评审），要么拒绝。

- 商店的长期形态与商业定位以 `whitepaper.md` §9 为权威，本文不重复论证。
- 页面清单与运行边界见 `app/README.md`。
- 本文回答的是：**当前阶段做到什么、明确不做什么、怎么判断自己没跑偏。**

---

## 1. 定位

**商店是秩序的发布与经营框架，不是任何一条具体秩序的内容。**

对应关系（Shopify 类比，仅作定位参照，不是产品目标）：

| Shopify | 本仓库 |
| --- | --- |
| 平台框架 | Store / Product Workbench 通用渲染与流程 |
| 商家 | 凝结核（秩序的设计者、组织者、运转者） |
| 商品 | 一条 Zhixu（链上 plan 锚 + 展示元数据 + 任务级声明 spec） |
| 消费者 | 供应商 / 执行者（浏览、申请加入、履约、交证据） |
| 支付清算（信任层） | 链上状态机与链事件（唯一协议事实） |

由此推出三条**永久红线**，任何期数都不得突破：

1. **框架不含内容。** 商店核心代码不得包含业务字段名、行业标签表、场景特判。
   行业知识只能以凝结核随秩序携带的数据（spec / 配置）存在。
   反例：报关单号/出口港口硬编码、`participantId==customs` 特判、中文标签匹配表。
2. **商店不是事实来源。** 合约与链事件是协议真相；目录、资料、装修、审核状态全部是
   可删除、可从链事件重建的投影。商店可以解释事实，不能成为事实。
3. **商店不代签名、不托管、不接管履约。** 标准化的是"确认"，不是履约本体；
   交易准备可以由商店完成，业务签名永远来自责任主体的钱包。

## 2. 分期形态

按期定性，不按功能堆叠；每期有验收口径，禁止"顺手把远期功能做了"。

| 期数 | 形态 | 范围 |
| --- | --- | --- |
| **MVP（现在）** | 单治理域、单运营方的秩序发布与加入闭环 | 本文 §3 的 A–D 四个目标 |
| 增长期 | 多凝结核入驻、店铺化经营页、秩序模板复用、试配层（signalMap 草案） | 另立文档，本文不展开 |
| 远期 | 白皮书 §9 完整秩序商店：行业方案市场、多治理域、外部服务提供者生态 | 见 whitepaper |

## 3. MVP 目标

MVP 的总验收口径：**一个陌生凝结核和一群陌生供应商，在零工程人员干预下，
跑通"上架 → 发现 → 理解 → 加入 → 履约 → 取证"。**

四个目标域，每个都有"算做到了"的口径：

### A. 秩序可以上架、展示、浏览

- 上架 = 导入编译产物（planId / planHash）+ 展示元数据（名称、描述、标签）
  + 任务级声明（evidenceSpec 等 spec 数据）。
- 展示 = 商品详情页：人类可读的秩序视图（阶段、角色、证据要求、责任声明）
  **加** 链上锚信息（planHash、状态机地址、identity registry、EIP-712 domain），
  且页面可核验 listing 与链上 plan 一致。
- 浏览 = 目录、搜索、筛选。

验收：详情页读者不读 DSL 也能回答三个问题——这条秩序是干嘛的、我扮演谁、我要交什么证据；
且页面上展示的秩序与链上注册事实可对上。

明确不做（MVP）：推荐排序、评分评论、SEO、模板市场。

### B. 可以加入秩序

- 供应商自助：浏览 → 申请角色槽位（提交自己的 supplier 身份与处理接口说明）。
- 凝结核侧：审核申请 → 批准后绑定到 role slot → 链上授权生效。
- 加入后的履约面：Product Workbench（订单、任务、证据上传、签名确认提交）。

验收：从"第一次见到这条秩序"到"出现在链上授权名单并收到第一个待办"，
中间不需要任何一方写代码、发邮件或线下拉群。

明确不做：自动撮合、代谈价格、合同文本生成、替任何一方决定是否加入。

### C. 链下身份审核 + 链上可信互动

这是商店存在的核心信任机制：**链下做治理动作，链上留可验证事实，两者必须成对出现。**

- 链下：KYB / 资质审核 / 能力标签是商店（治理域）的经营数据，商店对自己的审核结论负责。
- 链上：身份映射（`UVPIdentityRegistry`）、授权关系、plan 承诺（`commitPlan`/`finalizePlan`）、
  证据指纹。
- 商店与链的全部通信手段限定为三种：**读投影**（经 Chain Services）、
  **准备交易**（typed data / 交易参数）、**提交经营侧数据**（进商店自己的数据域）。
  签名永远发生在责任主体的钱包侧。

验收：商店消失后从链事件重建，参与方的授权与证据仍然成立；
页面上任何"可以信任谁 / 谁被授权了"的陈述，都能指出对应的链上表达
（哪个地址被授权了哪个 role slot），而不是只引用商店自己的记录。

明确不做：把审核状态上链、自建信任根、代管私钥、把商店数据库当权威。

### D. 最小治理

MVP 只治理"上架与入驻"，不治理"运行中的秩序"：

- 上架审核：版本、schema 校验、锚核验通过才可公开。
- 供应商入驻审核：链下资料审核 + 对应的链上授权动作。
- 下架与版本更新。
- 争议入口：商店只负责把人带到链上事实视图（回放、证据指纹、签名记录），不设仲裁庭。

验收：每个治理动作留有审计记录，且**只改变商店可见性与入驻资格，不改变链上事实**。

明确不做：争议裁决、单方改单、任何形式的跨域超级管理员权限（whitepaper §13.3）。

## 4. 装修模型（能力如何向凝结核开放）

三层开放，能力越强、信任风险越高，开放越谨慎：

1. **展示装修（theme）**：纯展示元数据——名称、描述、图文、标签。商店只管渲染。随时开放。
2. **交互装修（declarative spec）**：凝结核声明"这个任务要什么输入"（evidenceSpec：
   label / accept / required / inputKind / description），框架通用渲染。已落地。
   扩展规则：**新需求 = 新声明结构 = 扩 spec 字段或 inputKind 枚举（配结构校验），
   永远不是在框架里加业务 if。** spec 校验只做结构校验，永不编码业务预期。
3. **能力插件（apps）**：增长期再做。DTO 已预留（`SlotCapabilityPluginDTO`）。
   边界：插件 = 声明 pluginKind 与授权范围的 manifest，由框架或独立信任域渲染执行；
   **禁止凝结核向商店页面注入任意代码**——商店是治理面，必须保持 fail-closed。
   真正的执行插件装在协议适配层（periphery adapters），商店页只留声明与入口。

DSL 本身就是最高层级的装修：阶段、角色、权限、信号全是凝结核自定义的。
商店框架要回答的问题从来不是"允许用户定制多少功能"，而是
**"哪些自由度可以用纯数据表达"**。每遇到"某行业又特例了"，标准动作是
把那道业务自由度搬进声明数据层，而不是加一个分支。

## 5. 反跑偏自检（新需求五问）

1. 这是框架还是内容？→ 内容：做成 spec 数据 / demo 配置，不进框架代码。
2. 这个功能失效或删掉后，链上事实是否仍然完整？→ 否：商店越界成了事实来源，重做。
3. 它是否要求商店替某个主体做决定或签名？→ 是：拒绝。
4. 它属于 MVP 的 A/B/C/D 哪一项？→ 都不属于：记入增长期清单，不排进现在。
5. 这个特例能否用声明结构表达？→ 能：加 spec；不能、且确属普遍需求：
   升级为框架特性，同时修订本文。

## 6. 现状盘点（2026-09，随进度更新）

已有：目录/搜索/详情、草稿导入与编译发布、供应商资料页、docking、runtime 视图、
Product Workbench（含 evidenceSpec 通用渲染与多槽位证据上传）、经 Product API 的链上投影读取。

已有（2026-09 起具备）：

- A：上架域（listing 导入/锚核验/审核/下架/重新上架）+ 详情页锚核验面板；
  锚冲突显式告警并抑制加入入口。
- B：加入闭环（申请 → publisher 审核 → 身份配对 → 授权意向与链上 tx 证据 →
  投影观察到授权事件后 active）。
- C：钱包会话（SIWE 式签名锚定地址）+ 账号地址表 + descriptor 托管
  （`/identity/descriptors` 按 descriptorHash 可验证的 append-only 快照）。
- D：装修权限模型（planPublisher 权限根 + 委托表 + append-only 版本化）与
  上架/下架治理动作的审计记录。

剩余 MVP 缺口：

- 上架流与既有草稿编译发布流的并轨操作指引（当前两条入口并存）。
- 加入闭环在真实链（非模拟广播）上的端到端验收。

## 7. 与其他文档的关系

| 文档 | 职责 |
| --- | --- |
| `whitepaper.md` §9（uvp-eth） | 商店长期形态与商业定位（权威，冲突时以其为准） |
| 本文 | 本仓库的范围、期数与验收口径（做不做、先做不做，以本文为准） |
| `app/README.md` | 页面清单与运行边界 |
